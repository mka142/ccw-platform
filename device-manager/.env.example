# Device Manager Environment Configuration

# Database Configuration
# Use 'mongodb' for MongoDB connection, 'json' for JSON file storage (development/testing)
DB_TYPE=json

# MongoDB Configuration (used when DB_TYPE=mongodb)
MONGODB_URI=mongodb://root:example@localhost:27017/
MONGODB_DATABASE=ccw-platform

# Development Configuration
NODE_ENV=development
PORT=3000