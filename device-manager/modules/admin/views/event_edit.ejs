<!DOCTYPE html>
<html>
<%- include('_semantic_head', { title: 'Edytuj komunikat' }) %>
<body>
  <div class="ui menu blue">
    <div class="ui container">
      <a href="<%= url.admin %>/" class="header item">Panel Admina</a>
    </div>
  </div>
  <div class="ui main text container" style="margin-top:2em;">
    <div class="ui raised very padded text container segment">
      <h2 class="ui header">Edytuj komunikat</h2>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="ui negative message">
          <div class="header"><%= error %></div>
        </div>
      <% } %>
      <form class="ui form" method="POST" >
        <div class="field">
          <label>Typ komunikatu (eventType)</label>
          <input type="text" name="eventType" value="<%= event.eventType %>" required>
        </div>
        <div class="field">
          <label>Nazwa do czytania (label)</label>
          <input type="text" name="label" value="<%= event.label %>" required>
        </div>
        <div class="field">
          <label>Payload (JSON)</label>
          <textarea name="payload" rows="4"><%= JSON.stringify(event.payload) %></textarea>
        </div>
        <button type="submit" class="ui primary button">Zapisz</button>
        <a href="<%= url.admin %>/concerts/<%= event.concertId %>" class="ui button">Powrót</a>
      </form>
    </div>
  </div>
</body>
</html>
