/**
 * Shared application types
 * These re-export base types from lib and add application-specific extensions
 */

// Re-export base types from lib
export type { BaseEntity, BaseDocument, OperationResult } from "@/lib/types";
